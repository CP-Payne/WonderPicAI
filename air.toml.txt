root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/main ./cmd/app/main.go" # Simplest build
  bin = "./tmp/main"
  include_ext = ["go"] # Only watch Go files for now
  # Remove all exclude_dir, exclude_file, etc. for this test

[log] # Keep some basic color for output
  time = true
[color]
    main = "magenta"
    watcher = "cyan"
    build = "yellow"
    runner = "green"